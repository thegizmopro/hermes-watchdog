"""Hermes Gateway Watchdog for Windows (v2.1)Runs persistently, monitors gateway, restarts if down or frozen.Designed to be started via Scheduled Task (AtStartup trigger).v2.1 changes (2026-06-07):  - Primary health check uses gateway.pid file, not WMIC scan  - WMIC scan only used as fallback (fixes false dual-process detection)  - pythonw.exe + python.exe parent-child = ONE gateway (uv venv behavior)  - kill_gateway() uses `hermes gateway stop` (handles PID/state/scheduled task)  - start_gateway() uses `schtasks /Run` (single process chain, no duplicates)  - Explicit HERMES_HOME env var in all subprocess callsTwo-tier health check:  1. PID file check â?" is the process from gateway.pid alive?  2. Log freshness check â?" has gateway.log been written to recently?     (Catches "zombie" state: process alive but Telegram polling dead.)"""import subprocessimport timeimport osimport sysimport jsonfrom datetime import datetimeCHECK_INTERVAL = 10        # seconds between checksDOWN_THRESHOLD = 30        # seconds of process missing before restartingSTALE_THRESHOLD = 1800     # seconds of no log activity before declaring frozen (30 min)RESTART_COOLDOWN = 60      # minimum seconds between restartsMAX_RESTARTS_PER_HOUR = 5  # circuit breakerSTARTUP_GRACE = 120        # seconds after watchdog start before stale checks (handles sleep/wake)HERMES_HOME = r"C:\Users\kenzo\AppData\Local\hermes"HERMES_EXE = r"C:\Users\kenzo\AppData\Local\hermes\hermes-agent\venv\Scripts\hermes.exe"GATEWAY_PID_FILE = os.path.join(HERMES_HOME, "gateway.pid")GATEWAY_LOG = os.path.join(HERMES_HOME, "logs", "gateway.log")WATCHDOG_LOG = os.path.join(HERMES_HOME, "logs", "watchdog.log")GATEWAY_TASK = "Hermes_Gateway"# State files that can go stale and cause issuesSTATE_FILES = ["gateway.pid", "gateway.lock"]# For fallback WMIC scan onlyGATEWAY_MARKERS = ["gateway run"]GATEWAY_EXCLUDES = ["gateway_watchdog", "wmic"]down_since = Nonestale_since = Nonelast_restart = 0restart_times = []watchdog_start_time = time.time()def log(msg):    ts = datetime.now().strftime("%Y-%m-%d %H:%M:%S")    line = f"[{ts}] {msg}"    try:        sys.stdout.write(line + "\n")        sys.stdout.flush()    except Exception:        pass  # pythonw.exe has no stdout    try:        os.makedirs(os.path.dirname(WATCHDOG_LOG), exist_ok=True)        with open(WATCHDOG_LOG, "a") as f:            f.write(line + "\n")    except Exception:        passdef _hermes_env():    """Return a copy of os.environ with HERMES_HOME explicitly set."""    env = os.environ.copy()    env["HERMES_HOME"] = HERMES_HOME    return env# ---- PID-based health check (primary) ----def get_gateway_pid_from_file():    """Read the gateway PID from the state file."""    try:        with open(GATEWAY_PID_FILE, "r") as f:            data = json.load(f)            return data.get("pid")    except (OSError, ValueError, json.JSONDecodeError):        return Nonedef is_pid_alive(pid):    """Check if a Windows process exists by PID."""    try:        result = subprocess.run(            ["tasklist", "/FI", f"PID eq {pid}", "/NH", "/FO", "CSV"],            capture_output=True, text=True, timeout=5,            creationflags=0x08000000  # CREATE_NO_WINDOW        )        return str(pid) in result.stdout and "No tasks" not in result.stdout    except Exception:        return False# ---- WMIC-based fallback (for kill operations) ----def _find_gateway_pids_wmic():    """Return list of PIDs for processes that look like the gateway.    Used for force-kill operations, not for health checking."""    try:        result = subprocess.run(            ["wmic", "process", "where",             "(name='python.exe' or name='pythonw.exe')",             "get", "ProcessId,CommandLine"],            capture_output=True, text=True, timeout=10,            creationflags=0x08000000        )    except Exception as e:        log(f"WMIC error: {e}")        return []    found = []    for line in result.stdout.splitlines():        line = line.strip()        if not line or "ProcessId" in line:            continue        parts = line.rsplit(None, 1)        if len(parts) != 2:            continue        cmdline, pid_str = parts        try:            pid = int(pid_str)        except ValueError:            continue        cmdline_lower = cmdline.lower()        if any(m in cmdline_lower for m in GATEWAY_MARKERS) and \           not any(x in cmdline_lower for x in GATEWAY_EXCLUDES):            found.append(pid)    return founddef is_gateway_running():    """Check if gateway is alive using PID file (primary).    Falls back to WMIC scan if PID file is missing."""    # Primary: check PID file    pid = get_gateway_pid_from_file()    if pid is not None:        return is_pid_alive(pid)        # Fallback: scan for gateway processes (no PID file yet)    pids = _find_gateway_pids_wmic()    return len(pids) > 0def get_log_age():    """Return age of gateway.log in seconds, or None if file missing."""    try:        mtime = os.path.getmtime(GATEWAY_LOG)        return time.time() - mtime    except OSError:        return Nonedef kill_gateway():    """Stop the gateway cleanly using `hermes gateway stop`.    This handles: process termination, PID file cleanup, scheduled task stop.    Falls back to taskkill for any stragglers."""    # Step 1: Official stop command (stops scheduled task + drains process + cleans state)    try:        result = subprocess.run(            [HERMES_EXE, "gateway", "stop"],            capture_output=True, text=True, timeout=30,            env=_hermes_env(),            creationflags=0x08000000        )        output = (result.stdout + result.stderr).strip()        if output:            log(f"hermes gateway stop: {output}")    except subprocess.TimeoutExpired:        log("hermes gateway stop timed out, using force kill")    except Exception as e:        log(f"hermes gateway stop error: {e}")    # Step 2: Wait, then force-kill any surviving processes    time.sleep(3)    pids = _find_gateway_pids_wmic()    if pids:        log(f"Stragglers alive after stop, force-killing: {pids}")        for pid in pids:            try:                subprocess.run(                    ["taskkill", "/F", "/PID", str(pid)],                    capture_output=True, timeout=10,                    creationflags=0x08000000                )                log(f"Force-killed PID {pid}")            except Exception as e:                log(f"Error force-killing PID {pid}: {e}")        time.sleep(2)    # Step 3: Clean up stale state files    for state_file in STATE_FILES:        path = os.path.join(HERMES_HOME, state_file)        try:            if os.path.exists(path):                os.remove(path)                log(f"Removed stale {state_file}")        except Exception as e:            log(f"Could not remove {state_file}: {e}")def start_gateway():    """Start the gateway via the scheduled task.    Uses schtasks /Run to trigger Hermes_Gateway, which creates a single    pythonw.exe -> python.exe process chain (no duplicates)."""    try:        result = subprocess.run(            ["schtasks.exe", "/Run", "/TN", GATEWAY_TASK],            capture_output=True, text=True, timeout=15,            env=_hermes_env(),            creationflags=0x08000000        )        output = (result.stdout + result.stderr).strip()        if output:            log(f"Started {GATEWAY_TASK} task: {output}")        else:            log(f"Started {GATEWAY_TASK} task")    except Exception as e:        log(f"Failed to start gateway: {e}")def circuit_breaker_tripped():    """Return True if we've restarted too many times in the last hour."""    global restart_times    now = time.time()    restart_times = [t for t in restart_times if now - t < 3600]    return len(restart_times) >= MAX_RESTARTS_PER_HOURdef can_restart():    """Check cooldown and circuit breaker."""    if circuit_breaker_tripped():        log(f"Circuit breaker: {MAX_RESTARTS_PER_HOUR} restarts/hour exceeded - waiting")        return False    if time.time() - last_restart < RESTART_COOLDOWN:        remaining = RESTART_COOLDOWN - (time.time() - last_restart)        log(f"In cooldown ({remaining:.0f}s remaining)")        return False    return Truedef do_restart(reason):    """Kill (if needed) and restart the gateway."""    global last_restart, restart_times, down_since, stale_since    log(f"RESTARTING gateway: {reason}")    if is_gateway_running():        kill_gateway()    start_gateway()    last_restart = time.time()    restart_times.append(time.time())    down_since = None    stale_since = Nonedef main():    global down_since, stale_since, last_restart, restart_times    log(f"Watchdog v2.1 started (PID {os.getpid()}) - checking every {CHECK_INTERVAL}s "        f"(stale threshold: {STALE_THRESHOLD}s)")    while True:        try:            running = is_gateway_running()            in_grace = (time.time() - watchdog_start_time) < STARTUP_GRACE            # --- Check 1: Process alive ---            if not running:                stale_since = None  # irrelevant if process is dead                if down_since is None:                    down_since = time.time()                    log("Gateway process DOWN")                else:                    down_duration = time.time() - down_since                    if down_duration >= DOWN_THRESHOLD and can_restart():                        do_restart(f"process down {down_duration:.0f}s")                time.sleep(CHECK_INTERVAL)                continue            else:                # Process is alive                if down_since is not None:                    log("Gateway process recovered")                down_since = None            # --- Check 2: Log freshness (skip during startup grace) ---            if not in_grace:                log_age = get_log_age()                if log_age is not None and log_age > STALE_THRESHOLD:                    if stale_since is None:                        stale_since = time.time()                        log(f"Gateway log stale ({log_age:.0f}s since last write) - "                            f"possible zombie state")                    else:                        stale_duration = time.time() - stale_since                        if stale_duration >= 60 and can_restart():                            do_restart(f"log stale for {log_age:.0f}s "                                       f"(detected {stale_duration:.0f}s ago)")                            time.sleep(CHECK_INTERVAL)                            continue                else:                    if stale_since is not None:                        log("Gateway log active again - recovered")                    stale_since = None            time.sleep(CHECK_INTERVAL)        except KeyboardInterrupt:            log("Watchdog stopped by user")            break        except Exception as e:            log(f"Unexpected error: {e}")            time.sleep(CHECK_INTERVAL)if __name__ == "__main__":    main()